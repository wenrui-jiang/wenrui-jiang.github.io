# Experience Timeline Enhancement Plan

## 目标

将 `post` 版块从单纯的实习/项目经历列表，逐步升级为完整履历时间线，覆盖教育经历、实习经历、项目经历，并为每个学校或组织接入官网、简介和地图位置。

## 当前状态

- `content/*/post/` 已按 Hugo 内容条目组织经历。
- 每条经历通过 front matter 的 `date` 字段参与排序，可用于倒序时间线展示。
- 中文内容作为主稿，其余语言版本保守同步翻译。

## 后续接入字段

计划为教育经历和工作经历补充以下结构化信息：

- `official_site`: 学校或公司的官方网站。
- `location_name`: 展示用地点名称。
- `address`: 地图检索或展示地址。
- `geo`: 经纬度，格式建议为 `{ lat, lng }`。
- `summary`: 面向公开页面的简介。
- `type`: `education`、`work`、`internship` 等分类。
- `period`: 展示用起止时间。

## 页面能力规划

1. 时间线视图：按 `date` 倒序展示学习和工作轨迹，保留现有详情页。
2. 学校/组织卡片：显示官网入口、简短介绍、地点信息。
3. 地图视图：在经历页增加地图模块，展示各学校和工作地点的地理分布。
4. 筛选能力：按教育、工作、实习、项目等类型筛选。
5. 多语言同步：新增字段和文案时继续同步七种语言版本，避免不同语言页面信息不一致。

## 实施建议

- 第一阶段：在 Markdown front matter 中补充官网、地址、类型等结构化字段。
- 第二阶段：调整 Hugo 模板，让经历列表以时间线样式呈现。
- 第三阶段：接入静态地图或前端地图组件，优先使用公开安全的学校/机构地址。
- 第四阶段：为每个学校补充独立介绍，可从官网公开信息整理，避免引用隐私或非公开内容。
