# Project Global Memory

- 强制规则：任何内容结构、导航、文案、链接的修改，必须同步到全部语言版本（en/zh/es/fr/de/ar/la）。
- 执行定义：一次改动 = 七种语言都要检查并更新；不得只改单语。
- 页面定位：`Articles/文章` 指向 `portfolio`；`Project/项目` 指向 `about`。
- 项目页结构规则：每个项目条目包含「简介」「我做了什么」「访问链接（放在末尾）」。
- 品牌合规：公开页面不出现“尚医通”品牌名。
